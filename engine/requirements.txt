# Install from the engine/ directory:
#   python -m pip install -r requirements.txt
#
# This pulls the package plus the full research, GUI, broker-adapter, and test
# extras declared in pyproject.toml so local smoke/regression checks match the
# Streamlit + agent PM harness workflow.
-e .[all,dev]
