# Install from the engine/ directory:
#   python -m pip install -r requirements.txt
#
# Default install matches the offline research, GUI, and agent PM harness used by
# CI. Live broker connectivity is intentionally opt-in with:
#   python -m pip install -e .[broker]
-e .[data,ml,hrp,gui,dev]
